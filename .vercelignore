# Exclude dev-only files from the Vercel deployment bundle.
# NOTE: docs/ is NOT excluded — read at request time by force-dynamic Server Components.

# Dev scripts (not needed at runtime on Vercel)
scripts/

# Git internals
.git/
